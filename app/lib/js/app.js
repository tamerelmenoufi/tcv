Carregando = (opc = 'flex') => {
    $(".Carregando").css("display",opc);
    // alert(opc);
}

ultimaPosicao = 0;

function rolar(){
    var atualPosicao = window.scrollY;
    console.log(`${atualPosicao}`)
}

